# 部署指南 — 达哥成长记录网页

所有文件已准备好，Git 仓库已初始化并提交。请按以下步骤操作。

---

## 方式一：GitHub Pages（最简单）

### 第1步：在 GitHub 上创建仓库

1. 打开 https://github.com/new
2. Repository name 填：`dage-growth-record`
3. 选择 **Public**（公开，否则 Pages 无法免费使用）
4. **不要**勾选 "Add a README"、".gitignore"、"license"
5. 点击 **Create repository**

### 第2步：推送代码

打开终端，依次执行以下命令（把 `你的用户名` 替换成你的 GitHub 用户名）：

```bash
cd /Users/chencrystal/traework/达哥成长记录/deploy

# 设置 Git 用户名和邮箱（换成你自己的）
git config user.name "你的用户名"
git config user.email "你的邮箱"

# 添加远程仓库
git remote add origin https://github.com/你的用户名/dage-growth-record.git

# 推送代码
git push -u origin main
```

> 如果提示输入密码，需要用 GitHub Personal Access Token（不是登录密码）。
> 生成 Token：https://github.com/settings/tokens → Generate new token → 勾选 `repo` 权限

### 第3步：开启 GitHub Pages

1. 打开仓库页面，点击 **Settings**
2. 左侧菜单点击 **Pages**
3. Source 选择 **Deploy from a branch**
4. Branch 选择 **main**，文件夹选 **/ (root)**
5. 点击 **Save**
6. 等待 1-2 分钟，页面顶部会显示你的网址：
   `https://你的用户名.github.io/dage-growth-record/`

---

## 方式二：Vercel（速度更快，国内访问更好）

1. 推送代码到 GitHub（完成方式一的第1、2步）
2. 打开 https://vercel.com → 用 GitHub 账号登录
3. 点击 **Add New Project**
4. 选择你刚创建的 `dage-growth-record` 仓库
5. Framework Preset 选 **Other**（纯静态网页）
6. 点击 **Deploy**
7. 几秒后部署完成，获得网址：`https://dage-growth-record.vercel.app/`

---

## 方式三：Cloudflare Pages（国内访问最快）

1. 推送代码到 GitHub（完成方式一的第1、2步）
2. 打开 https://dash.cloudflare.com → 登录
3. 左侧菜单点击 **Workers & Pages**
4. 点击 **Create application** → **Pages** → **Connect to Git**
5. 选择 `dage-growth-record` 仓库
6. 构建命令留空，输出目录填 `/`
7. 点击 **Save and Deploy**
8. 获得网址：`https://dage-growth-record.pages.dev/`

---

## 备注

- 三种方式都是免费的
- 国内访问速度：Cloudflare Pages > Vercel > GitHub Pages
- 推送代码后 Vercel / Cloudflare Pages 会自动重新部署
- 网页文件在：`/Users/chencrystal/traework/达哥成长记录/deploy/`
